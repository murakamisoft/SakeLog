import React from 'react';
import { Alert } from 'react-bootstrap';

const PageNotFound = () => {
  return (
    <div>
      <Alert variant="danger">ページが存在しません。</Alert>
    </div>
  );
};

export default PageNotFound;
